// Custom JavaScript for the Student Management System
// (Currently empty as Bootstrap Bundle handles core JS functionality)

document.addEventListener('DOMContentLoaded', function () {
    // Add any custom JS logic here if needed in the future
    // (e.g., dynamic chart loading, custom delete confirmations)
});